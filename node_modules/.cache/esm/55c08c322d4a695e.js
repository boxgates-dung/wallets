let express,routes;_2d4‍.w("express",[["default",["express"],function(v){express=v}]]);_2d4‍.w("./routes",[["default",["routes"],function(v){routes=v}]]);


const app = express();

app.use(express.json());
app.use(express.urlencoded({ extended: false }));
routes(app, express.Router);

const PORT = 9000;
app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);
});
